Library Management System (Core Logic) – Issues Analysis

Class Relationships

* Book → Base class.
* DigitalBook extends Book.
* Member → Base class.
* PremiumMember extends Member.

Critical Errors

1. DigitalBook constructor missing super().
2. DigitalBook uses this before super().
3. Member.canBorrow() uses assignment (=) instead of comparison.
4. findMemberById() uses assignment (=) instead of comparison.
5. searchBooksByCategory() uses assignment (=) instead of comparison.
6. searchBooksByCategory() missing base case.
7. processReturnQueue() missing index increment.
8. borrowBook() does not verify member exists.
9. borrowBook() does not verify book exists.
10. Book constructor ignores copies parameter.
11. Missing availableCopies property.
12. Missing totalCopies property.

Major Issues

Object-Oriented Programming

* Missing Book availability method.
* Missing Book information method.
* Missing PremiumMember borrow override.
* Missing premium benefit properties.
* Missing Member.joinDate.
* Missing membership duration calculation.

Scoping & Variables

* Excessive var usage.
* Global mutable state (books, members).
* Function-scoped loop variables.

Loops & Conditionals

* Inefficient nested loops in findOverdueBooks().
* Wrong loop choice in findBookByISBN().
* Manual loops instead of filter(), find(), reduce().

Modern JavaScript

* Missing destructuring.
* Missing spread operator.
* Missing rest parameters.
* Missing template literals.
* Missing module exports.
* Missing Map/Set for ISBN lookups.

Missing Features

* Overdue book date logic.
* Availability validation before checkout.
* Statistics calculations.
* Error handling in borrowing workflow.

Fix Strategy

1. Fix runtime errors (super(), recursion, infinite loop).
2. Correct assignment/comparison mistakes.
3. Complete class implementations.
4. Add validation and error handling.
5. Refactor using modern array methods and ES6 features.
